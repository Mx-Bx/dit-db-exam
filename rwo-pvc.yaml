apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: single-access-pvc
  namespace: data-pipeline
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path

